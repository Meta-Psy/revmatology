worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout 65;

    # Ограничение размера загружаемых файлов (для uploads)
    client_max_body_size 20M;

    # Gzip-сжатие
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;

    # HTTP -> HTTPS редирект (включить после получения SSL)
    # server {
    #     listen 80;
    #     server_name rheumassociation.uz www.rheumassociation.uz;
    #
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    #
    #     location / {
    #         return 301 https://$host$request_uri;
    #     }
    # }

    # HTTPS (раскомментировать после получения SSL-сертификата)
    # server {
    #     listen 443 ssl;
    #     server_name rheumassociation.uz www.rheumassociation.uz;
    #
    #     ssl_certificate     /etc/letsencrypt/live/rheumassociation.uz/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/rheumassociation.uz/privkey.pem;
    #
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_prefer_server_ciphers on;
    #
    #     # Фронтенд (статика)
    #     location / {
    #         root /usr/share/nginx/html;
    #         index index.html;
    #         try_files $uri $uri/ /index.html;
    #     }
    #
    #     # API бэкенда
    #     location /api/ {
    #         proxy_pass http://backend:8000/api/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     # Загруженные файлы
    #     location /uploads/ {
    #         alias /app/uploads/;
    #         expires 30d;
    #         add_header Cache-Control "public, immutable";
    #     }
    #
    #     # Certbot
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    # }

    # HTTP-сервер (работает до получения SSL)
    # После получения SSL: закомментировать этот блок и раскомментировать два блока выше
    server {
        listen 80;
        server_name rheumassociation.uz www.rheumassociation.uz;

        # Certbot challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Фронтенд (статика)
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # API бэкенда
        location /api/ {
            proxy_pass http://backend:8000/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Загруженные файлы
        location /uploads/ {
            alias /app/uploads/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }
}
