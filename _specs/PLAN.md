# PLAN.md — корень древа планов revmatology

**Заведён:** 2026-09-13 · **Образец:** `Mentis/_specs/PLAN.md` (тот же формат, чтобы один прибор читал оба древа) · **Прибор:** пока `python C:/Users/Alex/10_Projects/Mentis/_tools/plan_model.py`-совместимый разбор; сверщик состояния к проекту не подключён — узел `О-06`.

Этот файл — единственный корень планов проекта. У любой идеи здесь есть место: она становится узлом с постоянным номером и родителем. Подробности живут в `_specs/*`, узел на них ссылается.

---

## Как устроен узел

| Поле | Правило |
|---|---|
| **№** | Постоянный. `К-01` остаётся `К-01` навсегда — на него ссылаются ветка, PR, коммит и субагент. Номера не переиспользуются даже после `закрыто` |
| **Узел** | Одна строка. Что должно стать правдой |
| **Состояние** | Только из перечня ниже. Записанное рукой состояние — гипотеза, пока нет прибора |
| **Родитель** | Обязателен. `корень` пишет только слой `Я-NN`; линия стоит на слое, рабочий узел — на линии или узле. Сирот нет |
| **Подробность** | Путь к design-doc в `_specs/`, либо `—` |

**Перечень состояний** (закрытый):

`замысел` → `вердикт дан` → `в работе` → `влито` → `выкачено` → `закрыто`

плюс `ждёт вердикта` (нужен ответ Alex, кодом не решается) и `не делаем` — **всегда с причиной**.

**Ярусы.** Слой `Я-NN` — рубрика с родителем `корень` и состоянием `—`. Линия `Л-NN` стоит на слое. Рабочие узлы стоят на линии: `К-` конгресс, `С-` контент сайта, `О-` обвязка (выкатка, CI, гигиена, древо), `Т-` сценарное тестирование. Клеток `Э-NN` и фактов `F-NN` в этом древе нет: заведём, когда появится второй прибор, который их меряет.

**Связь с коммитом.** Коммит, двигающий узел, несёт трейлер отдельной строкой **над** блоком `Co-Authored-By`:

```
Plan-Item: К-01
```

`влито` = трейлер найден в истории `main`. `выкачено` = коммит достижим из метки выкатки (`refs/deploys/prod`, ставится узлом `О-03`). До появления метки «выкачено» пишется рукой по факту зелёного деплоя и проверки `/api/health` — это оговорено в узле.

**Ствол репозитория:** `main` (`origin/HEAD → origin/main`). Ветки `fix/*`, `feat/*`, `docs/*`, `chore/*`; мерж только через PR, финальные ворота — Alex.

---

## Слои

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `Я-01` | Публичный сайт: то, что видит врач и участник конгресса на RU/UZ/EN | — | корень | — |
| `Я-02` | Админка: то, чем секретарь ассоциации наполняет сайт без разработчика | — | корень | — |
| `Я-03` | Выкатка: ствол = прод, и это проверяемо изнутри продукта | — | корень | — |
| `Я-04` | Приборы: правило живёт проверкой, которая краснеет | — | корень | — |

## Линии

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `Л-01` | Конгресс: программа, спикеры, регистрация — от админки до публичной страницы | в работе | `Я-01` | `_specs/2026-09-13-congress-quality-audit-design.md` |
| `Л-02` | Контент сайта: правление, центры, болезни, новости, школы, hero-фоны | замысел | `Я-02` | — |
| `Л-03` | Выкатка и обвязка: CI, версия, откат, гигиена репозитория, древо | в работе | `Я-03` | `_specs/2026-09-13-congress-quality-audit-design.md` §2 |
| `Л-04` | Сценарное тестирование: процесс как единица проверки, не функция | в работе | `Я-04` | `_specs/processes.yaml` |

## Л-01 · Конгресс

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `К-01` | День программы создаётся с датой; `''` в датах/времени = null; 404 на несуществующего родителя; секции по `congress_id`; перенос секции; первая pytest-инфраструктура | выкачено | `Л-01` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). §4 P0.1. Ветка `fix/congress-program-schemas`, `2aee256`, 16 pytest, два ревью пройдены. Корень бага: поле `date` затеняло тип → `NoneType` |
| `К-02` | Админка: `allSettled` при загрузке, секции всех дней в форме спикера, перенос секции между днями, `required` доходит до `<input>`, сценарный тест «день → секция → доклад» | выкачено | `К-01` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). §4 P0.2. Ветка `fix/congress-admin-sections`, `800cbfe` поверх `b0afcaf` (без ребейза: сведена с `К-01` merge-коммитом в интеграционной ветке). Попутно найдено и починено: `loadCongressData` откатывал выбранный день на первый после любого сохранения; селекторы объявлялись внутри рендера |
| `К-03` | Страница `/congress/:id/program`: вся программа на одной странице по языкам, доклады вне секций, текст/файл программы; карточки спикеров с фото 3:4 | выкачено | `Л-01` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). §4 P1. Ветка `feat/congress-program-page`, `50246d8`, 107 vitest, два ревью пройдены. Файл по языкам подписан честно как «информационное письмо»; отдельная колонка `program_file_*` — не заведена, ждёт вердикта в `К-06`-соседнем узле при необходимости |
| `К-04` | Ресайз изображений при загрузке: белый список типов, лимит, max 1600 px, HEIC→JPEG | выкачено | `Л-01` | Влито PR #15. Выкачено 2026-09-14: Deploy run 34863353576 (CI + выкатка) зелёный; прод `/api/congress/congresses/1/detail` отдаёт `program_file_*` (миграция `003` применена). PR #15 (`feat/upload-image-resize`), ревью пройдено с правками; решение Alex 2026-09-14 — до конгресса. §4 P2.5 |
| `К-05` | Публичная регистрация защищена: rate-limit, дедуп по email+конгресс, пагинация списка регистраций | замысел | `Л-01` | §4 P3 |
| `К-06` | `is_active` у дней/секций, время/зал у секции, «доклад» как сущность отдельно от спикера | ждёт вердикта | `Л-01` | §4 P3. Нужно ли это конгрессу-2027 — решает Alex |
| `К-07` | Программа конгресса в PDF по языкам: `program_file_*` + миграция `003`, вкладка «Программа» в админке, инфо-письмо загрузкой вместо URL | выкачено | `Л-01` | Влито PR #13. Выкачено 2026-09-14: Deploy run 34863353576 (CI + выкатка) зелёный; прод `/api/congress/congresses/1/detail` отдаёт `program_file_*` (миграция `003` применена). PR #13 (`feat/congress-program-pdf`), ревью пройдено с правками (гонки загрузок). Решение Alex 2026-09-14 «PDF + спикеры» |
| `К-08` | Конкурс молодых учёных: PDF положения по языкам + страница `/congress/:id/young-scientists`; общий просмотр PDF страницами-картинками (WebP 800/1600) для конкурса и программы | выкачено | `Л-01` | Влито PR #16 (сервер), #17 (фронт), #19 (нестабильный тест) 2026-09-14. Выкачено 2026-09-14: Deploy run 34880668280 зелёный; бандл прода `index-ecqh91jb.js` = локальная сборка `cd726ed`; в контейнере pypdfium2 5.13.0, Pillow 12.3.0; `alembic current` = `004`. Проверка на реальных телефонах — после загрузки настоящего PDF. `_specs/2026-09-14-young-scientists-pdf-design.md`. Ветки `feat/pdf-pages-backend`, `feat/pdf-pages-frontend` от `base/k08` (#14+#13+#15) |
| `К-09` | Публичные страницы не качают код админки: ленивый кусок `AdminApp` через `React.lazy`, сторож сборки (модули админки во входе и бюджет gzip) | выкачено | `Л-01` | Влито PR #20 (`f1c89be`). Выкачено 2026-09-15: Deploy run 34905383715 зелёный; бандл прода `index-Cenq8yf8.js` = локальная сборка `f1c89be`, кусок `AdminApp-DEZ8jSsV.js` отдаётся отдельно (134 242 байт). Замер (vite build): вход было 212.8 КБ gzip → стало 186.1 КБ, кусок админки 28.7 КБ; на проводе (nginx gzip 1) 246.8 → 214.2 КиБ. Бюджет входа `ENTRY_GZIP_BUDGET` = 192 КиБ. Поручение Alex 2026-09-15, срок до заморозки 2026-09-21. Ветка `feat/admin-lazy-chunk`. `_specs/2026-09-15-admin-lazy-chunk-design.md` |
| `К-10` | Публичные страницы ленивыми кусками: во входе только оболочка сайта и главная; boundary на сбой загрузки куска; сторож сборки (страницы и просмотр PDF вне входа), бюджет входа опущен под новый замер | выкачено | `К-09` | Влито PR #22 (`63653dc`). Выкачено 2026-09-15: Deploy run 34912810288 зелёный; бандл прода `index-DVHTGe-w.js` (469 030 байт) = локальная сборка `63653dc`, куски `Congress-BH0-Fo-x.js`, `AdminApp-B6mSK-lo.js` отдаются отдельно; безголовый Chrome на проде: `/`, `/congress/1`, `/congress/1/program` (PDF), `/about` (редирект) рисуются с шапкой и подвалом, без ошибки загрузки куска. Вход 186.07 → 139.21 кБ gzip (−25 %), бюджет `ENTRY_GZIP_BUDGET` = 146 КиБ; на проводе `/` 238.3 → 179.9 КиБ. Ревью: 4 находки исправлены (редирект с пустым `main`, тайм-ауты тестов пути сбоя). Решение Alex 2026-09-15: главная остаётся во входе. Срок — заморозка 2026-09-21. Ветка `feat/public-lazy-pages`. `_specs/2026-09-15-public-lazy-pages-design.md` |
| `К-11` | Именные сертификаты участникам: список получателей из CSV, поиск с подсказками + телефон, имя каллиграфией на PDF-шаблоне, ≤ 5 выдач; вкладка «Сертификаты» в админке | выкачено | `Л-01` | Выкачено 2026-09-22: Deploy run 35695825503 зелёный (`4c8f5e6`); с прода `certificates/status` → `{"open":false}`, `suggest` → `[]`, `issue` → 404, админские без токена → 401, в бандле `index-Da8hJ6Sy.js` есть кусок `CongressCertificate`. Влито PR #24. Шрифт Caveat 500, порядковый номер «No.» без повторов (`next_number`), рамки по умолчанию под бланк организаторов. `_specs/2026-09-22-congress-certificates-design.md`, план `_specs/2026-09-22-congress-certificates-plan.md`. ТЗ организаторов 2026-09-22. PR #24 (ветка `feat/congress-certificates`, фронт сведён merge-коммитом из `feat/congress-certificates-fe`), 2026-09-22: 321 pytest, 304 vitest, ревью пройдено (15 находок исправлены). Ждёт мержа Alex, затем реальный шаблон и список от организаторов. Срок не горит; nginx/compose не трогаем до 2026-09-27 (`О-09`) — частота в приложении, шаблон в БД (не в публичном `/uploads`) |
| `К-12` | Сертификаты в личном кабинете: страница `/profile` «Мои сертификаты» по совпадению email учётной записи с email получателя; получатели одной кнопкой из регистраций на конгресс; выдача открывается сама на следующий день после окончания (`issue_mode` auto/open/closed вместо `is_open`) | ждёт мержа | `К-11` | Решения Alex 2026-09-22: кабинет плюс прежняя страница поиска, получатели — все зарегистрированные, автооткрытие. Миграция `006` (email получателя, `issue_mode`), правило открытия — `backend/functions/certificate_schedule.py` (Asia/Tashkent), сценарий `P-08`. Принятый риск: email учётной записи не подтверждается, поэтому знающий чужую почту получит чужой сертификат — лимит 5 выдач общий с поиском. Выкатка в два шага (§8 дизайна): `006` только расширяет схему и оставляет `is_open`, `deploy.yml` собирает образ → гонит `db_upgrade.py` разовым контейнером → поднимает backend, поэтому окна с 500 нет. Попутно починен вход: почта нормализуется, поиск пользователя регистронезависим, `get_current_user` смотрит `is_active` (403) — без этого человек с почтой в верхнем регистре не дошёл бы до кабинета. 2026-09-25: 429 pytest, 354 vitest, lint и сборка зелёные, ревью пройдено. `_specs/2026-09-22-certificates-account-design.md` |
| `К-13` | Сузить схему после К-12: миграция `007` снимает `certificate_templates.is_open`; заодно нормализовать существующие почты в `users` и завести уникальный индекс по `lower(email)` (сейчас от дублей по регистру защищает только проверка в `/register`) | ждёт выкатки К-12 | `К-12` | Второй шаг «расширить, потом сузить»: пишется и выкатывается только после того, как выкатка `006` подтверждена на проде. Кандидат в тот же узел: индекс `ON users (lower(email))` — сейчас вход делает seq scan (таблица крошечная, не горит) |

## Л-02 · Контент сайта

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `С-01` | `src/test/i18n-test-utils.js` содержит JSX под `.js` и не импортируется из тестов; переименовать в `.jsx`, тесты страниц используют общий init | выкачено | `Л-02` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). сделано внутри ветки `К-03` (`50246d8`), закроется её мержем |
| `С-02` | `dangerouslySetInnerHTML` на контенте админки (`Congress.jsx`, три места + страница программы): санитизировать или принять как есть | ждёт вердикта | `Л-02` | контент пишет только админ; XSS-поверхность есть |
| `С-03` | Ключи-сироты в локалях (`program.selectDay`, `sponsors.empty`, …) и `t` без использования в `News.jsx`/`NewsDetail.jsx` | замысел | `Л-02` | отчёт аудита §1.5, линт |
| `С-04` | Админка «Члены правления» и «Главные ревматологи» рисуют пустой список: `emptyIcon` передаётся компонентом, а не элементом | выкачено | `Л-02` | Влито PR #21 (`bb5977f`). Выкачено 2026-09-15: Deploy run 34912764036 зелёный, поверх — 34912810288 (`63653dc`, бандл прода = локальная сборка, правка в куске `AdminApp-B6mSK-lo.js`). Найдено при `К-09` 2026-09-15: `<Users/>`/`<Stethoscope/>` элементом в `EmptyState` → React #130 на пустом списке (на проде списки непусты). Ветка `fix/admin-empty-icon`, тест `AdminPages.emptyList.test.jsx` красный до правки |

## Л-03 · Выкатка и обвязка

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `О-01` | Единое древо проекта: `PLAN.md`, `processes.yaml`, карта каталогов, трейлер `Plan-Item` в коммитах | выкачено | `Л-03` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). этот файл; `revmatology-map.json`; ветка `docs/congress-quality-audit`. Коммиты узлов до 2026-09-14 трейлер не несут (у `К-*` его нет, у `docs(plan)` он отделён пустой строкой от `Co-Authored-By`); PR #11 влит squash'ем (`8e4ce2c`), и строки `Plan-Item` всех узлов оказались в середине тела: `git log --grep` их находит, `%(trailers)` — нет. Прибору `О-06` искать по тексту |
| `О-02` | CI на `pull_request`: frontend lint + test + build, backend pytest (включая `tests/workflows`); деплой только после зелёного CI | выкачено | `Л-03` | Влито PR #14. Выкачено 2026-09-14: Deploy run 34863353576 (CI + выкатка) зелёный; прод `/api/congress/congresses/1/detail` отдаёт `program_file_*` (миграция `003` применена). PR #14 (`chore/ci-pr-checks`), CI зелёный на самом PR. §4 P2.1. Сейчас линт падает: 20 ошибок в 13 файлах |
| `О-03` | Версия изнутри продукта: `/api/health` с `git_sha`, sha в футере, smoke после деплоя (`sha прода == GITHUB_SHA`, иначе job красный), `pg_dump` до `alembic upgrade`, метка `refs/deploys/prod` | замысел | `Л-03` | §4 P2.2 |
| `О-04` | Seed конгресса для локального стенда (конгресс, 2 дня, 3 секции, 5 спикеров с UZ/EN) + `.env.example` с `POSTGRES_*` | замысел | `Л-03` | §4 P2.3 |
| `О-05` | Гигиена: `backend/venv`, `*.pyc`, `rheumatology.db` вон из индекса; удалить `origin/master` и влитые ветки; пути в `.claude/settings.local.json`; `DEPLOY.md` с актуальным IP/юзером | замысел | `Л-03` | §4 P2.4. venv — 4128 файлов в индексе |
| `О-06` | Сверщик состояния Mentis читает и это древо: параметризовать `Mentis/_tools/audit_state.py` (имена репо, `NOT_DEPLOYED`, пути фактов) или вынести общий `plan_model` | ждёт вердикта | `Л-03` | правка в репозитории Mentis, не здесь; без неё состояния в этом файле — рукописная гипотеза |
| `О-07` | Alembic: baseline-миграция, снятая с прода, и `alembic check` в CI против дрейфа моделей | замысел | `О-02` | §4 P3. Таблицы конгресса живут только через `create_all` |
| `О-08` | `echo=False` на движке (SQL с ПДн в логах), `class Config` → `model_config` в схемах | замысел | `Л-03` | §4 P3 |
| `О-09` | nginx: кэш `/assets` на год (`immutable`), `index.html` без кэша, `http2 on;`, `nginx -t` в deploy.yml перед перезапуском | замысел | `Л-03` | Решение Alex 2026-09-15: после конгресса, не раньше 2026-09-27 (прод-инфраструктуру до конца конгресса не трогаем). Прод сейчас: nginx 1.29.5, HTTP/1.1, gzip вкл., у `/assets` нет Cache-Control, повторные заходы идут через 304 |

## Л-04 · Сценарное тестирование

| № | Узел | Состояние | Родитель | Подробность |
|---|---|---|---|---|
| `Т-01` | Реестр процессов `_specs/processes.yaml` + харнесс `backend/tests/workflows/` (Journey, registry, гейт: у каждого `active` процесса есть прогон на каждый заявленный слой) | выкачено | `Л-04` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). той же веткой, что `Т-02`; образец `Mentis/mentis.uz/backend/tests/workflows/`, дизайн `Mentis/mentis.uz/_specs/2026-08-13-workflow-testing-design.md` |
| `Т-02` | P-01 «Секретарь собирает программу конгресса»: journey по HTTP от логина до публичной программы на трёх языках | выкачено | `Т-01` | Выкачено 2026-09-14: Deploy run 34811062016 зелёный; бандл прода `index-DkP2xRaU.js` совпал с локальной сборкой того же дерева; `/congress/1/program` рисуется без ошибок консоли. Влито PR #11 (интеграционный `release/congress-program-2026-09`, 2026-09-14). `processes.yaml` P-01. Ветка `test/p01-congress-journey`, `5ef112d` поверх `2aee256`, 43 pytest; на коде до `К-01` падает на звене `create_days` (422) |
| `Т-03` | P-02 «Участник регистрируется на конгресс»: journey по HTTP | замысел | `Т-01` | `processes.yaml` P-02 |
| `Т-04` | Браузерный слой (Playwright) для P-01: секретарь проходит путь через реальную админку | замысел | `Т-02` | нужны `О-04` (seed) и `О-02` (куда запускать) |
| `Т-05` | P-03…P-05 (новость, фото, заявка в школу): journey по HTTP | замысел | `Т-01` | `processes.yaml` |
| `Т-06` | Сценарный тест админки `CongressAdmin.workflow.test.jsx` укладывается в тайм-аут в полном прогоне vitest | выкачено | `Л-04` | Влито PR #14. Выкачено 2026-09-14: Deploy run 34863353576 (CI + выкатка) зелёный; прод `/api/congress/congresses/1/detail` отдаёт `program_file_*` (миграция `003` применена). в PR #14: CPU-профиль — 64 % в `getComputedStyle` jsdom; тест ~4× быстрее. 2026-09-14: в полном прогоне «день → секции двух дней → спикер…» падает по тайм-ауту 30 с, отдельно проходит за 23 с. Нестабильный прибор хуже отсутствующего |

## Очередь исполнения

Срок-ограничитель: конгресс LEAR стартует **2026-09-25**.

1. `К-01` → `К-02` (зависимость по API), параллельно `К-03` — до 2026-09-18. Вместо трёх PR — один интеграционный PR #11: squash подряд конфликтует, а параллельные выкатки без `concurrency` гоняются за `frontend/dist`.
2. `Т-01` + `Т-02` поверх `К-01` — тот же срок: P-01 должен краснеть на коде до `К-01`.
3. `О-01` — этот коммит.
4. `О-02`, `О-03` — до 2026-10-01: без них `выкачено` остаётся рукописным.
5. `О-04`, `О-05`, `К-04` — до 2026-10-15.
6. `К-09` — до заморозки 2026-09-21 (выкачено 2026-09-15); `К-10` — до заморозки 2026-09-21 (выкачено 2026-09-15); `О-09` — после 2026-09-27.
7. Остальное — по вердиктам.
